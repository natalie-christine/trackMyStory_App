<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="tMS.Pages.TasksSettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:Popups="clr-namespace:tMS.Pages"
    >

    <toolkit:Popup.Resources>
        <Style TargetType="{x:Type Popups:TasksSettingsPage}">
            <Setter Property="Size" Value="500,420" />
            <Setter Property="Color" Value="Transparent" />
            <Setter Property="HorizontalOptions" Value="Center" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="CanBeDismissedByTappingOutsideOfPopup" Value="False" />
        </Style>
    </toolkit:Popup.Resources>

    <Grid Padding="20" BackgroundColor="Transparent">
        <Border StyleClass="AppTheme">
            <toolkit:DockLayout ShouldExpandLastChild="False">
                <VerticalStackLayout toolkit:DockLayout.DockPosition="Top" Spacing="10">
                    <Label
                        FontSize="Medium"
                        HorizontalOptions="Center"
                        Text="Einstellungen"
                        VerticalOptions="Center" />
                    <Label Text="Hier sollen Einstellungsdinge passieren" />
                    <Button Text="Kategorie Name ändern" />
                    <Button Text="Kategorie - Farbe ändern" />
                    <Button Text="Kategorie - löschen" />

                </VerticalStackLayout>

                <FlexLayout
                    Margin="10"
                    toolkit:DockLayout.DockPosition="Bottom"
                    HeightRequest="50">
                    <Button
                        Command="{Binding SaveCommand}"
                        FlexLayout.Grow="1"
                        Text="OK" />
                    <Button
                        Clicked="btnClose_Clicked"
                        FlexLayout.Grow="1"
                        Text="Abbrechen" />
                </FlexLayout>
            </toolkit:DockLayout>
        </Border>
    </Grid>
</toolkit:Popup>