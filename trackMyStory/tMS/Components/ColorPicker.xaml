<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="tMS.Components.ColorPicker"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:tMS.Components"
    x:DataType="components:ColorPicker">

    <VerticalStackLayout Padding="10,0,20,0" VerticalOptions="Center">
        <Label x:Name="labelRed" Text="Rot:" />
        <Slider
            x:Name="sliderRed"
            ValueChanged="Slider_ValueChanged"
            Value="{Binding Red}" />
        <Label x:Name="labelGreen" Text="Grün:" />
        <Slider
            x:Name="sliderGreen"
            ValueChanged="Slider_ValueChanged"
            Value="{Binding Green}" />
        <Label x:Name="labelBlue" Text="Blau:" />
        <Slider
            x:Name="sliderBlue"
            ValueChanged="Slider_ValueChanged"
            Value="{Binding Blue}" />
    </VerticalStackLayout>


</ContentView>
